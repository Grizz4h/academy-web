# Public Vite env for RinQ frontend (never put service-role / JWT secrets here)
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=

# Unified progression pipeline (Phase 5, default on). Set 0 only for emergency rollback.
# VITE_PROGRESSION_UNIFIED_PIPELINE=0
