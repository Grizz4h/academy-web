# Competency Engine Simulations (Design Only)

**Not production runtime code.**

- `engine_v1_sim.py` — deterministic V1 engine prototype for Phase 4A design validation.
- Run: `python backend/competency/simulations/engine_v1_sim.py`
- Tests: `python -m unittest test_competency_engine_simulation`

Remove or relocate before production engine lands in `backend/competency/engine/`.
